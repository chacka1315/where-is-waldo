// module.exports = {
//   presets: [['@babel/preset-env', { targets: { node: 'current' } }]],
// };

const config = {
  presets: [['@babel/preset-env', { targets: { node: 'current' } }]],
};

export default config;
