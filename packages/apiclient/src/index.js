import { getAboard, getAllBoards, getLeaderboard } from './boards';
import { getACharacter, getAllCharacters } from './characters';
import { submitScore, startRound, validateXY } from './rounds';

export default {
  getAboard,
  getAllBoards,
  getACharacter,
  getAllCharacters,
  submitScore,
  startRound,
  validateXY,
  getLeaderboard,
};
